# Simulated Peer Review: Paper A

_"The Listening Geometry: Where Audio-Language Models Listen, Guess, and Collapse"_

Generated: deterministic review simulation

---

## Reviewer 1: Methodology Critic

**Overall Score: 5/10**

**Summary:** The paper proposes gc(k), a causal grounding coefficient for ALMs, embedded in a five-dimensional Listening Geometry framework. The theoretical contribution is clear, but the empirical validation is critically weak: only 2 real experiments on the smallest model scale, with the remaining 27 experiments using a mock framework.

### Strengths

1. gc(k) is well-defined mathematically via DAS-IIA, with clear connection to causal abstraction theory (§3.1). The formalization is rigorous.
2. The AND/OR gate framework (§3.2) provides a principled mechanistic decomposition of multimodal feature dependence, going beyond layer-level analysis.
3. Pre-registered predictions (§4.8) are commendable and show intellectual honesty — few interpretability papers make falsifiable predictions about concurrent work.
4. Honest reporting of blocked experiments Q117 and Q123 (§4.9) builds credibility.

### Weaknesses

1. **Critical: Mock/Real experiment gap.** 7 of 9 experiments use a mock framework (§4.3–4.7). Mock experiments validate internal consistency of the formalism but cannot demonstrate that gc(k) captures real neural network behavior. The two real experiments (Q001, Q002) use only Whisper-base (74M params). This is acknowledged in §5.5 but not sufficiently mitigated — the paper reads as a theoretical proposal masquerading as an empirical contribution.
2. **Baselines are insufficient.** The paper compares gc(k) conceptually to AudioLens and Beyond Transcription (§5.1) but never runs these methods on the same data for direct quantitative comparison. Without head-to-head baselines, the claim that gc(k) 'subsumes and extends' prior metrics (§5.1) is unsupported.
3. **Statistical claims require scrutiny.** The r=0.98 correlation between AND% and gc(k) (§4.3) is from mock data where both quantities are generated by the same framework — this is a consistency check, not an empirical finding. The 96% suppression detection rate (§4.6) and other metrics from mock experiments should not be cited as validation of the framework's predictive power.
4. **Ablations are missing.** How sensitive is gc(k) to: (a) number of minimal pairs, (b) DAS rotation training set size, (c) choice of phonological contrast, (d) subspace dimensionality m? None of these ablations appear in the paper.
5. **Paper is incomplete.** 78 [TODO] markers remain in the text, including missing data in core results tables (§4.1, §4.2). Several key results (real gc(k) sweep, RAVEL on real SAE features, Qwen2-Audio experiments) are listed as 'awaiting GPU access.' This paper is not ready for review.

### Questions for Authors

Q1. Why was Whisper-base chosen for the real experiments instead of Whisper-small, which is the stated 'primary analysis target' (§3.4.1)? Was this purely a compute constraint?
Q2. The Q001 result shows weak voicing alignment (cos_sim = +0.25 for stop-stop at layer 5). Is this strong enough to support the claim that 'linearly structured phonological representations can serve as intervention targets'? What is the expected cos_sim for a reliable DAS intervention?
Q3. How does gc(k) behave when the audio is completely uninformative (e.g., silence or white noise)? Is gc(k) = 0 at all layers in this case, or does it depend on text context?
Q4. The mock framework is described only indirectly. Can you provide a concise formal specification of what the mock model computes? Without this, it is impossible to assess whether mock results generalize.
Q5. In Q002, every single-layer ablation produces WER ≈ 1.0. Does this mean gc(k) will be uniformly high across all layers (since every layer is necessary), which would make the 'listen layer' concept vacuous for Whisper?

### Specific Suggestions

1. [HIGH] (§4.3) Run gc(k) sweep on Whisper-small with at least 500 minimal pairs and report the full curve with bootstrap CIs. This single experiment would transform the paper.
2. [HIGH] (§5.1) Add a baseline comparison: run AudioLens information score on the same stimuli and show where gc(k) and AudioLens agree/disagree. This would substantiate the 'causal upgrade' claim.
3. [HIGH] (§3.3) Provide an explicit formal definition of the mock framework in §3 or an appendix. Currently the reader must infer what mock experiments actually compute.
4. [MEDIUM] (§3.1) Report sensitivity of gc(k) to DAS subspace dimensionality m. Even a small sweep (m ∈ {1, 2, 4, 8, 16}) on Whisper-base would help.
5. [MEDIUM] (§4.5 / §5.3) The H4 failure (§4.5, variance ratio = 0.073) deserves more discussion. If persona effects are small relative to within-condition variability, the safety implications (§5.3) are overstated.

---

## Reviewer 2: Novelty Assessor

**Overall Score: 6/10**

**Summary:** The paper introduces a principled causal framework (gc(k) + Listening Geometry) for understanding audio grounding in ALMs. The framing is novel and the theoretical connections are well-drawn. However, the core contribution — applying DAS to audio-language models — is a straightforward extension of existing text-domain methods (Geiger et al., 2021/2023; RAVEL) to a new modality. The AND/OR gate framework adds conceptual value but its empirical validation is entirely synthetic.

### Strengths

1. The gc(k) metric fills a genuine gap: no prior work provides causal, layer-wise audio grounding quantification for ALMs. The related work (§2) convincingly argues this gap exists across four independent research threads.
2. The five-dimensional Listening Geometry and four-profile taxonomy (strong/shallow/fragile listener, sophisticated guesser) is a creative framing that could become a standard vocabulary for the field if validated.
3. The cross-paper predictions (§5.4) for MPAR² and Modality Collapse are specific, falsifiable, and demonstrate the framework's generative power beyond the immediate experiments.
4. Connecting AND/OR gates to safety implications (§5.3) — particularly the insight that jailbreaks operate by inducing gc suppression — is novel and practically relevant.
5. The paper honestly positions itself relative to the closest methodological analog (FCCT for vision-LLMs, §2.2), making clear what is borrowed vs. new.

### Weaknesses

1. **Incremental methodology.** gc(k) = IIA_audio / (IIA_audio + IIA_text) is a straightforward ratio of DAS-IIA scores. The theoretical contribution is applying an existing causal framework (DAS/IIT) to a new domain (audio). This is valuable engineering but modest in terms of methodological novelty.
2. **AND/OR gate results may be trivial.** The mock finding that 'all features at gc peak are AND-gates' (§4.4) is almost tautological: by definition, the layer with highest audio dependence should have features that require audio. The interesting question is what happens at *non-peak* layers, and this is unexplored.
3. **Five dimensions, three validated.** The Listening Geometry claims five dimensions but defers two (CS, t*) to Paper B (§5.5). A framework paper that validates only 60% of its proposed dimensions will face skepticism about the unvalidated parts.
4. **No comparison to simpler alternatives.** Would a simple linear probe for 'audio content present' at each layer produce similar layer-wise profiles to gc(k)? Without ablating the complexity of DAS, it is unclear whether the full causal machinery is necessary to achieve the paper's stated goals.

### Questions for Authors

Q1. How does gc(k) differ from simply computing mutual information between layer-k representations and the audio input? What does the causal (interventional) perspective buy that an observational metric would not?
Q2. The paper claims gc(k) provides both sufficiency and necessity evidence (Contribution 1). Can you clarify exactly which experiments test sufficiency vs. necessity? The DAS intervention appears to test only sufficiency (patching in source activations).
Q3. Is the Listening Geometry truly five-dimensional, or are the dimensions highly correlated? The r=0.98 between α_AND and gc suggests they may be near-redundant.
Q4. What would change in the framework if AND-gates were a continuum rather than a binary classification? The δ threshold makes the classification discrete — is there a continuous version?

### Specific Suggestions

1. [HIGH] (§4 / §5.1) Add a 'simple baseline' comparison: linear probe accuracy for audio content at each layer vs. gc(k). If they correlate highly, discuss what gc(k) adds beyond probing; if they diverge, highlight the cases.
2. [MEDIUM] (§4.4) Consider splitting the AND/OR gate results to show non-peak layers in detail. The transition zone (layers adjacent to k*) is where the interesting biology of multimodal integration happens.
3. [HIGH] (§3 / §5.5) Either validate all five Listening Geometry dimensions in this paper, or rename the framework to reflect what is actually validated (e.g., 'Grounding Geometry' with three dimensions). Claiming five dimensions while delivering three invites rejection.
4. [MEDIUM] (§4.2 / §1) Strengthen the novelty narrative by emphasizing what is *surprising* in the results. The Q002 finding (every layer ablation → WER ≈ 1.0) is genuinely surprising and distinguishes audio from text models. Lead with this.

---

## Reviewer 3: Clarity & Presentation

**Overall Score: 5/10**

**Summary:** The paper is ambitious in scope and well-structured at the outline level. However, the draft is clearly incomplete — numerous TODOs, missing data tables, and a placeholder conclusion undermine readability. The writing quality in completed sections is generally strong, but notation inconsistencies and an imbalanced section structure detract from clarity.

### Strengths

1. The related work (§2) is comprehensive and well-organized into four clear threads, each ending with a 'Gap' statement that motivates the contribution. This is excellent scholarly writing.
2. The progression from gc(k) definition (§3.1) to AND/OR gates (§3.2) to experimental protocol (§3.3) follows a logical build-up that a reader can follow.
3. The abstract is dense but complete — it covers all five dimensions, key results, and applications in a single paragraph. At ~250 words it is appropriate for a top venue.
4. Tables are used effectively for presenting results (when data is present). The intervention procedure (§3.3.2) is described step-by-step with admirable precision.

### Weaknesses

1. **78 TODO markers remain.** These appear in critical locations including results tables (§4.1, §4.2), threshold values (§3.2.1), dataset sizes (§3.3.1, §3.4.2), and the entire conclusion (§6). The paper cannot be evaluated in this state.
2. **Notation inconsistency for gc.** The following forms appear: gc, gc(f, k), gc(k), gc(k*), gc(k^*). The paper uses gc, gc(k), gc(k*), gc(L), and related notation without a consistent typographic convention. Suggestion: always use $gc(k)$ with italic 'gc' and reserve gc without argument only when referring to the metric generically.
3. **Section imbalance.** Estimated word counts: Related Work §2 is very long relative to Results §4. The related work exhaustively catalogs prior work but could be tightened by 30–40% without losing essential context. Results §4 is sparse due to missing data — once filled, rebalance.
4. **Missing conclusion.** §6 contains only TODO placeholders. A paper without a conclusion is structurally incomplete. Even a draft conclusion summarizing the three validated dimensions would help reviewers.
5. **No figures.** The paper references no numbered figures. A gc(k) curve plot, an AND/OR gate visualization, and a Listening Geometry profile comparison are essential for reader comprehension. At minimum, add: (1) a schematic of the intervention procedure, (2) a mock gc(k) curve with annotation, (3) the four-profile taxonomy as a 2D plot.

### Questions for Authors

Q1. The abstract mentions '29 experiments (2 real-model on Whisper, 27 mock-framework validation)' but the body text refers to '2 real experiments and 20 mock-framework experiments across 5 analysis axes' (§1, Contribution 4). Which count is correct?
Q2. Is 'Schelling stability (σ)' defined anywhere in the paper? It appears in the abstract and §1 as a Listening Geometry dimension but I cannot find its definition in §3.
Q3. The CITE placeholders (e.g., [CITE: Zhao 2602.23136]) — are these arXiv IDs? Please convert to standard citation format before submission.
Q4. What is 'MicroGPT' (§4.7)? It appears in the RAVEL experiment but is never defined or cited.

### Specific Suggestions

1. [HIGH] (Throughout) Resolve all TODO markers before submission. Prioritize: (1) results data in §4.1 and §4.2 tables, (2) threshold values δ and ε in §3.2.1, (3) conclusion in §6.
2. [HIGH] (§3–§4) Add at least 3 figures: (1) Intervention procedure schematic, (2) gc(k) curve example with annotated k*, (3) Four-profile taxonomy visualization. Consider also: AND/OR gate Venn diagram, mock vs real comparison plot.
3. [MEDIUM] (§3.1.2) Standardize gc notation. Define a typographic convention in §3.1.2 and enforce it. Suggested: $\mathrm{gc}(k)$ for the layer-indexed metric, $k^*$ for the listen layer, $\alpha_{\text{AND}}$ for AND-gate fraction.
4. [MEDIUM] (§2) Tighten §2 Related Work by ~30%. Each subsection currently ends with a 'Gap' statement — these are excellent, but the preceding paragraphs could be more concise. Consider merging §2.1 and §2.2 since both cover speech model interpretability.
5. [LOW] (Abstract / §4) Reconcile experiment counts between abstract and body. Add a summary table listing all experiments (ID, type, model, status) as an appendix.
6. [MEDIUM] (§3 / Abstract) Define Schelling stability (σ) in §3 or remove it from the abstract. Currently it appears without definition.

---

## Combined Action Items (Prioritized)

### P0: Must Fix Before Submission

1. **[Reviewer 1]** (§4.3) Run gc(k) sweep on Whisper-small with at least 500 minimal pairs and report the full curve with bootstrap CIs. This single experiment would transform the paper.
2. **[Reviewer 1]** (§5.1) Add a baseline comparison: run AudioLens information score on the same stimuli and show where gc(k) and AudioLens agree/disagree. This would substantiate the 'causal upgrade' claim.
3. **[Reviewer 1]** (§3.3) Provide an explicit formal definition of the mock framework in §3 or an appendix. Currently the reader must infer what mock experiments actually compute.
4. **[Reviewer 2]** (§4 / §5.1) Add a 'simple baseline' comparison: linear probe accuracy for audio content at each layer vs. gc(k). If they correlate highly, discuss what gc(k) adds beyond probing; if they diverge, highlight the cases.
5. **[Reviewer 2]** (§3 / §5.5) Either validate all five Listening Geometry dimensions in this paper, or rename the framework to reflect what is actually validated (e.g., 'Grounding Geometry' with three dimensions). Claiming five dimensions while delivering three invites rejection.
6. **[Reviewer 3]** (Throughout) Resolve all TODO markers before submission. Prioritize: (1) results data in §4.1 and §4.2 tables, (2) threshold values δ and ε in §3.2.1, (3) conclusion in §6.
7. **[Reviewer 3]** (§3–§4) Add at least 3 figures: (1) Intervention procedure schematic, (2) gc(k) curve example with annotated k*, (3) Four-profile taxonomy visualization. Consider also: AND/OR gate Venn diagram, mock vs real comparison plot.

### P1: Should Fix

8. **[Reviewer 1]** (§3.1) Report sensitivity of gc(k) to DAS subspace dimensionality m. Even a small sweep (m ∈ {1, 2, 4, 8, 16}) on Whisper-base would help.
9. **[Reviewer 1]** (§4.5 / §5.3) The H4 failure (§4.5, variance ratio = 0.073) deserves more discussion. If persona effects are small relative to within-condition variability, the safety implications (§5.3) are overstated.
10. **[Reviewer 2]** (§4.4) Consider splitting the AND/OR gate results to show non-peak layers in detail. The transition zone (layers adjacent to k*) is where the interesting biology of multimodal integration happens.
11. **[Reviewer 2]** (§4.2 / §1) Strengthen the novelty narrative by emphasizing what is *surprising* in the results. The Q002 finding (every layer ablation → WER ≈ 1.0) is genuinely surprising and distinguishes audio from text models. Lead with this.
12. **[Reviewer 3]** (§3.1.2) Standardize gc notation. Define a typographic convention in §3.1.2 and enforce it. Suggested: $\mathrm{gc}(k)$ for the layer-indexed metric, $k^*$ for the listen layer, $\alpha_{\text{AND}}$ for AND-gate fraction.
13. **[Reviewer 3]** (§2) Tighten §2 Related Work by ~30%. Each subsection currently ends with a 'Gap' statement — these are excellent, but the preceding paragraphs could be more concise. Consider merging §2.1 and §2.2 since both cover speech model interpretability.
14. **[Reviewer 3]** (§3 / Abstract) Define Schelling stability (σ) in §3 or remove it from the abstract. Currently it appears without definition.

### P2: Nice to Have

15. **[Reviewer 3]** (Abstract / §4) Reconcile experiment counts between abstract and body. Add a summary table listing all experiments (ID, type, model, status) as an appendix.

---

## Meta-Analysis

**Average Score: 5.3/10**

### Paper Statistics

| Metric | Value |
|--------|-------|
| Word count | 8,469 |
| TODO markers | 78 |
| CITE placeholders | 47 |
| Tables | 11 |
| Figures | 0 |
| Equations | 6 |
| Real experiments | 2 |
| Mock experiments | 7 |
| Claims | 8 |
| Conclusion written | No |

### Submission Readiness Assessment

**BLOCKERS (must resolve before submission):**

- 78 TODO markers remain in text
- Conclusion section is empty
- Only 2 real experiments (vs. 7 mock)
- No numbered figures in the paper

### Score Interpretation

Scores suggest the paper has a strong conceptual core but requires substantial empirical strengthening before submission to a top venue. Consider targeting a workshop or completing the real experiments first.
