# Cycle c-20260317-1515
**Action:** plan | **Phase:** explore-fallback | **Track:** T3+T5

## Context
Build budget = 0, ideate = 0 for today. arXiv search unresponsive.
Pivoted to medium plan: prioritize tomorrow's 2 build slots.

## Priority Plan for 2026-03-18

### Build slots (2 available on reset)
1. **Q113** — `cascade_degree_mock.py`: Cascade degree = 1 - AND_gate_fraction at gc peak
   - Pearson r > 0.8 → validates Cascade Equivalence Hypothesis
   - Core evidence for Paper A Claim 2
   - Mock data: gc(k) per layer, AND-gate fraction per layer; compute r
2. **Q112** — extend `online_delta_gs_monitor.py`: M9 causal consistency window
   - Dual alarm: M9 drop AND delta_gs spike
   - Extends existing working T5 script → fast build

### Overflow (days with > 2 build budget)
3. Q107 (T3): isolate_gc_proxy.py — RAVEL Isolate argmin ≈ gc peak savings
4. Q89 (T3): temporal AND/OR gate onset distribution
5. Q083 (T5): M9-gated dual-factor adversarial detector

### Design doc cluster (plan-budget cycles)
- Q116: Backdoor=cascade induction design doc
- Q095: OBLITERATUS × T5 safety signature audit  
- Q108: Jailbreak = Isolate peak leftward shift design doc
- These are feasible as plan-budget actions (no build slots consumed)

## Key Insight
Q113 + Q107 together close the mechanistic chain for Paper A:
- Q113: cascade degree ↔ AND-gate fraction (mechanistic quantification)
- Q107: RAVEL Isolate argmin ≈ gc peak (computational equivalence)
Both are CPU Tier-0 mocks, fast to implement.
