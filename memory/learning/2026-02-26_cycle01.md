# 🧠 Learning Cycle #1 — 2026-02-26 14:22

## 論文：UniWhisper: Efficient Continual Multi-task Training for Robust Universal Audio Representation
- **arXiv:** 2602.21772 (2026-02-25)
- **領域:** cs.SD, cs.AI
- **作者:** Yuxuan Chen et al.

## 問題
現有 audio encoder 通常在單一領域（語音/環境音/音樂）表現好，但跨域會退化。如何用**單一 encoder** 同時捕捉 fine-grained speech cues 和 high-level semantics？

## 方法核心
- **Unified instruction-answer format**：把異質 audio tasks 統一成 instruction → answer 格式
- **Standard next-token training**：不需要 task-specific heads/losses
- **Continual multi-task training**：在 38k hours public audio 上訓練
- **評估方式**：shallow MLP probes + kNN，跨 20 個 tasks（speech / env sound / music）

## 結果
| 方法 | MLP Probe (NWA) | kNN (NWA) |
|------|-----------------|-----------|
| Whisper | 0.64 | 0.46 |
| UniWhisper | **0.81** | **0.61** |

保持 strong speech performance 的同時大幅提升跨域能力。

## 與我的研究的關聯
- **AudioMatters 直接相關**：都在做 audio representation / evaluation
- 他們用 instruction format 統一 heterogeneous tasks，和 AudioMatters 的 benchmark 設計思路可以互補
- kNN probe 作為評估方式值得參考（lightweight, no fine-tuning）
- 38k hours training data → 可以比較我們 benchmark 覆蓋的 task 範圍

## 延伸問題
1. Continual training 是否有 catastrophic forgetting？他們怎麼處理？
2. 20 tasks 的選擇是否有 bias？和 AudioMatters 的 task coverage 比較？
3. Instruction format 對 encoder representation quality 的影響機制是什麼？

## Tags
#audio #representation-learning #multi-task #whisper #benchmark
