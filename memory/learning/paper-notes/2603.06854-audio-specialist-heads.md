# Paper Note: Are Audio-Language Models Listening?
> arXiv:2603.06854 | Glazer, Aharon, Fetaya | 2026-03-06

## TL;DR
用 mechanistic interpretability 找到 audio-specialist attention heads，建構 audio-silence steering direction，inference-time activation intervention → Qwen LALMs 在 MMAU 上 +8pp，無需參數更新。

## Key Ideas
1. **Audio-specialist heads**: 少量 attention heads 的 audio attention 構成 "listening signal"
2. **Listening signal**: 當 audio evidence 實際影響 model output 時，signal 增加
3. **Steering direction**: audio vs silence 的 activation difference → 用來放大 audio effect
4. **Inference-time intervention**: 在 final representation 加上 steering direction

## 和我們工作的關係
- **同一個現象**: 他們在 attention head level 找 audio engagement，我們的 gc(k) 在 layer level 找 listen-vs-guess
- **差異/我們的優勢**:
  - gc(k) 是 **causal attribution**（因果歸因），不只是 heuristic identification
  - 我們的方法 layer-level → 更 coarse 但更 interpretable
  - 他們的方法 head-level → 更 fine-grained
- **互補性**: 可以引用他們作為 concurrent work，強調我們的 causal 角度
- **Paper A §2**: 需要在 related work 中明確定位——我們的 gc(k) vs 他們的 audio-specialist heads

## Action Items
- [ ] 細讀 full paper（PDF）
- [ ] 比較他們的 MMAU evaluation 和我們的 metrics
- [ ] 考慮在 Paper A 中引用並做 positioning
- [ ] 和智凱哥討論：這篇對我們投 arxiv 的 framing 有影響嗎？

## Tags
mechanistic-interpretability, audio-LLM, attention-heads, activation-steering, Qwen, MMAU
